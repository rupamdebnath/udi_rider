
class DirectionDetails
{
    String distanceText;
    String durationText;
    int distanceValue;
    int durationValue;
    String encodedPoints;

    DirectionDetails({this.distanceText, this.durationText, this.durationValue, this.distanceValue, this.encodedPoints});
}